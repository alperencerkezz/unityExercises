using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WhoIsHere : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("I'm here!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
